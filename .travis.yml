language: python
python:
- '3.8'
install:
- pip install -r requirements.txt
- pip install -r tests/requirements.txt
- pip install -e .
cache:
- pip
script:
- pytest -vx --cov=laplace/ tests/
after_success:
- coveralls
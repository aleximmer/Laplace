# Laplace

## Setup

```bash
pip install -r requirements.txt
# for development
pip install -e .
# for "production"
pip install .


# run tests (once we have them..)
pip install pytest
pytest tests/
```

